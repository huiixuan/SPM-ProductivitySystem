[pytest]
# Tell pytest where to find tests
testpaths = tests

# Add backend folder to PYTHONPATH so `import app` works
pythonpath = .

# Default coverage options
addopts = --cov=app --cov-report=term-missing --cov-report=xml
